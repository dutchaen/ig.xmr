export const HOSTNAME = '127.0.0.1:3000';

export const HTTPS_PREFIX = `http://${HOSTNAME}`;
export const WEBSOCKET_PREFIX = `ws://${HOSTNAME}`;

export const DEFAULT_PROFILE_PHOTO = HTTPS_PREFIX + '/default-profile-photo';
